# This session-mode windows layout requires tmux.
mode: session

windows:
  - name: dev
    panes:
      - command: opencode
        focus: true
      - command: mise run bootstrap
        split: horizontal
  - name: tests
    panes:
      - command: |
          vp test --watch
      - command: pitchfork logs dev --follow
        split: horizontal
